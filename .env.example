# When running frontend standalone (cloned separately), set this so API calls reach the backend:
# VITE_API_URL=http://localhost:3000
#
# When running from monorepo with proxy, leave unset (uses same-origin proxy).
# If your backend uses /api prefix (e.g. POST /api/register), set:
# VITE_API_URL=/api
